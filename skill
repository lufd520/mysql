# MySQL 活跃线程飙高排查指南

## 适用场景

- MySQL 活跃线程（Threads_running）突然飙高
- 业务反馈数据库响应变慢
- 监控告警活跃会话数超阈值

## 排查思路（按优先级）

### 第一步：确认现象

```sql
-- 查看线程状态分布（最关键的一步）
SELECT command, state, COUNT(*) as cnt 
FROM information_schema.processlist 
WHERE command != 'Sleep' 
GROUP BY command, state 
ORDER BY cnt DESC LIMIT 20\g

-- 查看总体线程数
SHOW GLOBAL STATUS LIKE 'Threads_%'\g
```

**关键判断点**：看堆积在哪个 state 上。不同的 state 指向不同的根因：

| 堆积状态 | 可能原因 |
|----------|----------|
| `query end` / `starting` | semi-sync 等待 ACK、binlog fsync 瓶颈 |
| `Waiting for table metadata lock` | DDL 阻塞或长事务持有 MDL |
| `Sending data` | 慢查询、缺索引、IO 瓶颈 |
| `Waiting for lock` / `LOCK WAIT` | 行锁等待、死锁 |
| `Opening tables` | table_open_cache 不足 |
| `Creating sort index` | 大量排序操作、缺索引 |
| `copy to tmp table` | DDL 在线操作 |

### 第二步：根据 state 深入排查

#### 情况 A：大量 `query end` 堆积

这是本次遇到的问题。`query end` 表示 SQL 已执行完但卡在提交阶段。

**排查路径**：

1. 检查 semi-sync 状态：
```sql
SHOW GLOBAL STATUS LIKE 'Rpl_semi_sync%'\g
SHOW GLOBAL VARIABLES LIKE 'rpl_semi_sync%'\g
```

关注指标：
- `Rpl_semi_sync_master_status` = ON 且 `Rpl_semi_sync_master_wait_sessions` > 0 → 确认是 semi-sync 问题
- `Rpl_semi_sync_master_no_times` 在增长 → semi-sync 正在频繁降级

2. 检查 redo log 刷盘：
```sql
SHOW GLOBAL STATUS WHERE Variable_name IN 
('Innodb_log_waits', 'Innodb_os_log_pending_writes', 'Innodb_os_log_pending_fsyncs')\g
```
- 如果 `Innodb_log_waits > 0` → redo log 空间不足导致等待
- 如果 `Innodb_os_log_pending_writes > 0` → 磁盘 IO 瓶颈

#### 情况 B：大量 `Sending data`

```sql
-- 查看长时间运行的查询
SELECT id, user, host, db, time, state, LEFT(info, 200) as sql_text 
FROM information_schema.processlist 
WHERE command != 'Sleep' AND time > 5 
ORDER BY time DESC LIMIT 20\g
```

#### 情况 C：锁等待

```sql
-- 查看 InnoDB 事务和锁
SELECT trx_id, trx_state, trx_started, trx_rows_locked, trx_rows_modified, trx_query 
FROM information_schema.innodb_trx 
ORDER BY trx_started LIMIT 10\g

-- 查看锁等待（5.7）
SELECT * FROM information_schema.innodb_lock_waits\g

-- 查看锁等待（8.0）
SELECT * FROM performance_schema.data_lock_waits\g
```

### 第三步：确认复制链路健康

```sql
-- 查看从库状态（如果当前实例也是从库）
SHOW SLAVE STATUS\G

-- 查看 Binlog Dump 连接
SELECT id, user, host, command, time, state 
FROM information_schema.processlist 
WHERE command LIKE 'Binlog%'\g
```

### 第四步：确认无其他瓶颈

```sql
-- InnoDB 关键指标
SHOW GLOBAL STATUS WHERE Variable_name IN (
'Innodb_buffer_pool_wait_free',
'Innodb_row_lock_current_waits', 
'Innodb_row_lock_time_avg',
'Innodb_log_waits')\g

-- 连接和中断
SHOW GLOBAL STATUS WHERE Variable_name IN (
'Aborted_clients', 'Aborted_connects', 
'Threads_connected', 'Threads_running')\g
```

## 本次案例复盘

### 现象
- 阿里云 RDS（新加坡区）活跃线程间歇性飙到 400~600
- 呈脉冲式：正常 20~30 → 突然飙到 400+ → 1 秒后恢复

### 根因
Semi-sync 复制（AFTER_SYNC 模式）的从库间歇性 ACK 超时（timeout=1000ms），导致：
1. 所有写入事务在 `query end` 阶段等待 ACK
2. 1 秒内堆积约 400+ 写入线程
3. 超时后降级为异步，堆积瞬间释放
4. 恢复 semi-sync 后再次超时...循环

### 证据
- 385 个线程卡在 `query end`，197 个在 `starting`
- `Rpl_semi_sync_master_status` = ON
- `Rpl_semi_sync_master_no_times` 持续增长（220 → 224）
- processlist 中有 `Waiting for semi-sync ACK from slave` 状态
- `Innodb_log_waits` = 0（排除 redo log 瓶颈）

### 处理
关闭 semi-sync 后活跃线程立即稳定在 20~30，无尖峰。

### ACK 慢的原因分析
- 正常 ACK 延迟仅 600 微秒（很健康）
- 间歇性超时，平均每 11 小时一次（近期加剧）
- 可能原因：从库 IO 瞬时瓶颈（checkpoint/flush）或跨可用区网络微抖动

### 后续建议
1. 恢复 semi-sync 时将 timeout 从 1000ms 调到 3000~5000ms
2. 联系云厂商检查只读实例 IO 性能
3. 或保持异步复制（接受极小概率数据丢失风险）

## 关键经验

1. **`query end` 堆积 = 提交阶段卡住**，首先想到 semi-sync 和 binlog fsync
2. **脉冲式尖峰**（突然飙高又突然恢复）是 semi-sync 超时降级的典型特征
3. **semi-sync AFTER_SYNC 模式**下，timeout 设置过小 + 高并发写入 = 灾难性堆积
4. 排查时先看 state 分布，不要只看 Threads_running 数字
5. RDS 环境下无法 SSH 到从库，只能通过主库侧的 semi-sync 状态指标推断
